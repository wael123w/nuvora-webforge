# User-request header analysis

The attached image measures 794×218 pixels and was read in two overlapping horizontal tiles, from left to right.

| Verified observation | Requested change |
|---|---|
| A white native Windows menu/title area appears above the dark application header. The visible menu labels are `File`, `Edit`, `View`, and `Window`. | Remove this native white top bar completely. |
| The product wordmark should show only the selected commercial name. | Use `Nuvora WebForge` consistently with no visible release label. |

No other reliable visual requirement was present in the supplied reference.
